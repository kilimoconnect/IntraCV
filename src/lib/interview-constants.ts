/**
 * Single source of truth for interview quota constants.
 * Import from here — never hard-code these values elsewhere.
 */
export const FREE_QUOTA = 5;
export const PAID_BATCH = 20;
export const INTERVIEW_UNLOCK_AMOUNT = 5;
export const INTERVIEW_UNLOCK_CURRENCY = "USD";
