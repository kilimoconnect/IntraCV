# Supabase project configuration
# https://supabase.com/docs/guides/cli/config

[project]
# Your Supabase project ID — found in: Dashboard → Project Settings → General
# project_id = "your-project-id"

# ── Edge Functions ─────────────────────────────────────────────────────────────

# [functions.cleanup-unconfirmed-users]
# Scheduled via pg_cron SQL migration (see supabase/migrations/20260407_cleanup_unconfirmed_cron.sql)
# Upgrade CLI to v2.84+ to use the schedule key here instead.
